# kusaku-flask

1. Make sure you run the program in python version 3.8
2. Run "pip install -r requirements.txt"
3. Run train_and_save_mode.py
4. And your model is saved in same directory as "model_weights.h5"
5. And your model evaluation is saved too in same directory as "evaluation.png" and "evaluation.csv"